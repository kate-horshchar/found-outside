# Docker Compose input: copy to .env (git-ignored). Compose passes TAX_RATE_PERCENT to the
# app with no default; local Java/Maven do not read this file.
# Integer 0-100. Missing or invalid: store and health work, quote/checkout return CONFIG_MISSING.
TAX_RATE_PERCENT=20
